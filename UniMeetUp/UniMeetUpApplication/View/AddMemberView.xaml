<UserControl x:Class="UniMeetUpApplication.View.AddMemberView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UniMeetUpApplication.View"
             xmlns:viewModel="clr-namespace:UniMeetUpApplication.ViewModel" Initialized="largerFontByDefault"
             mc:Ignorable="d" 
             Name="UserControlName">

    <UserControl.Resources>

        <viewModel:AddMemberViewModel x:Key="addMemberViewModel"></viewModel:AddMemberViewModel>

      </UserControl.Resources>
    <Grid Background="White" DataContext="{StaticResource addMemberViewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
<Grid.RowDefinitions>
    <RowDefinition Height="Auto"></RowDefinition>
    <RowDefinition Height="Auto"></RowDefinition>
</Grid.RowDefinitions>


        <Label Grid.Row="0" Grid.Column="0" FontWeight="Bold">Users:</Label>
        <!--<DataGrid Grid.Row="1" ItemsSource="{Binding Path=EmailCollectionAllUserEmails}"  
                  CanUserResizeColumns="False" IsSynchronizedWithCurrentItem="True">
            <DataGrid.Columns>
                <DataGridTemplateColumn  Width="SizeToCells" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Add member"></Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

        </DataGrid>-->

        <DataGrid  GridLinesVisibility="Horizontal" Grid.Row="1" Name="DG1" ItemsSource="{Binding Path=EmailCollectionAllUserEmails}" AutoGenerateColumns="False" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="User Email:" Width="80*"  Binding="{Binding EmailAddress}"/>
                 
                    <!--The Email property contains a URI.  For example "mailto:lucy0@adventure-works.com"-->
                    
                    <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button >Add member</Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            </DataGrid>

        <Grid.Resources>

            <!--<Style TargetType="{x:Type TextBox}">
                <Setter Property="Margin" Value="5"/>
            </Style>-->
            <!--<Style TargetType="{x:Type DataGridCellsPanel}">
                <Setter Property="Margin" Value="5"/>
            </Style>-->
        </Grid.Resources>

    </Grid>




</UserControl>
